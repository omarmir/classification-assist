{
  "name": "classification-assist",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "prepare": "nuxt prepare",
    "prepare:runtime": "node scripts/prepare-local-runtime.mjs",
    "build:single": "npm run prepare:runtime && npm run generate && node scripts/inline-single-html.mjs"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@iconify-json/lucide": "^1.2.105",
    "@nuxt/ui": "^4.7.1",
    "mammoth": "^1.12.0",
    "nuxt": "^4.4.4",
    "vue": "^3.5.34"
  },
  "devDependencies": {
    "playwright-core": "^1.59.1"
  }
}
